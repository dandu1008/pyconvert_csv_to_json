============
Installation
============

At the command line::

    $ pip install pyconvert_csv_to_json

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pyconvert_csv_to_json
    $ pip install pyconvert_csv_to_json
